<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:GitExt.UI.ViewModels"
             xmlns:views="using:GitExt.UI.Views"
             xmlns:controls="using:GitExt.UI.Controls"
             mc:Ignorable="d"
             d:DesignWidth="918" d:DesignHeight="622"
             x:Class="GitExt.UI.Views.WorkingTreeView"
             x:DataType="vm:WorkingTreeViewModel"
             Loaded="OnViewLoaded">

    <!--
        YERLEŞİM GitExtensions'ın `FormCommit`'inden BİREBİR alındı (CLAUDE.md § 9).
        Kaynak: src/app/GitUI/CommandsDialogs/FormCommit.Designer.cs

          splitMain  (dikey)   : Panel1 = sol sütun            │ Panel2 = sağ sütun
                                 SplitterDistance = 397 / 918  → sol ≈ %43
          splitLeft  (yatay)   : Panel1 = Unstaged (üst)
                                 Panel2 = toolbarStaged + Staged (alt)
                                 SplitterDistance = 274 / 622  → üst ≈ %44
          splitRight (yatay)   : Panel1 = SelectedDiff (üst)
                                 Panel2 = commit mesajı (alt, P05-T12)
                                 SplitterDistance = 426 / 622  → üst ≈ %68

        Araç çubuğu (`toolbarStaged`) iki listenin ARASINDA duruyor ve öğeleri iki yana
        hizalı: unstage eylemleri solda, stage eylemleri sağda (`ToolStripItemAlignment.Right`).
        Yönler listelerin konumuyla uyumlu — "Stage" yukarıdaki listeden aşağıdakine taşıyor.

        ⚠️ AYRI BİR "untracked" BÖLÜMÜ YOK: GitExtensions'ta takip edilmeyen dosyalar
        Unstaged listesinde duruyor. Plan üç bölüm öngörüyordu; § 9 kuralı gereği yerleşim
        kazandı (ayrıntı: WorkingTreeViewModel).
    -->

    <UserControl.Resources>
        <!--
            Satır biçimi P04-T08'deki değişen dosyalar listesiyle AYNI: git'in kendi durum
            harfi + yol. Kullanıcı harfleri zaten biliyor; iki listede iki ayrı gösterim
            kurmak gereksiz bir alfabe olurdu.
        -->
        <DataTemplate x:Key="WorkingTreeFileTemplate" DataType="vm:WorkingTreeFileRow">
            <Grid ColumnDefinitions="Auto,*" Height="20">
                <TextBlock Grid.Column="0"
                           Text="{Binding StatusLetter}"
                           Classes="status"
                           Classes.added="{Binding IsAdded}"
                           Classes.deleted="{Binding IsDeleted}"
                           Classes.conflicted="{Binding IsConflicted}"
                           Width="16"
                           FontFamily="Cascadia Mono,Consolas,Menlo,DejaVu Sans Mono,monospace"
                           FontSize="12"
                           VerticalAlignment="Center" />

                <TextBlock Grid.Column="1"
                           Text="{Binding Path}"
                           FontSize="12"
                           TextTrimming="CharacterEllipsis"
                           VerticalAlignment="Center"
                           Margin="4,0,0,0" />
            </Grid>
        </DataTemplate>
    </UserControl.Resources>

    <UserControl.Styles>
        <Style Selector="TextBlock.status">
            <Setter Property="Foreground" Value="{DynamicResource SystemControlForegroundBaseMediumBrush}" />
        </Style>
        <Style Selector="TextBlock.status.added">
            <Setter Property="Foreground" Value="#1A7F37" />
        </Style>
        <Style Selector="TextBlock.status.deleted">
            <Setter Property="Foreground" Value="#CF222E" />
        </Style>
        <Style Selector="TextBlock.status.conflicted">
            <Setter Property="Foreground" Value="#BC4C00" />
        </Style>

        <!-- Konu sayacı sınırı aşınca uyarı rengine geçiyor; commit'i ENGELLEMİYOR. -->
        <Style Selector="TextBlock.over">
            <Setter Property="Foreground" Value="#BC4C00" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
    </UserControl.Styles>

    <Grid ColumnDefinitions="43*,Auto,57*" Margin="6">

        <!-- ================= SOL SÜTUN ================= -->
        <Grid Grid.Column="0" RowDefinitions="44*,Auto,56*">

            <!-- ═══ Üst: Unstaged ═══ -->
            <Grid Grid.Row="0" RowDefinitions="Auto,*">

                <TextBlock Grid.Row="0"
                           Name="UnstagedCaption"
                           Text="{Binding Unstaged.Count, StringFormat='Unstaged changes ({0})'}"
                           FontWeight="SemiBold"
                           FontSize="12"
                           Margin="2,0,0,4" />

                <Border Grid.Row="1"
                        BorderThickness="1"
                        BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}">

                    <ListBox Name="UnstagedList"
                             ItemsSource="{Binding Unstaged}"
                             SelectedIndex="{Binding SelectedUnstagedIndex}"
                             ItemTemplate="{StaticResource WorkingTreeFileTemplate}"
                             Background="Transparent"
                             BorderThickness="0"
                             GotFocus="OnUnstagedFocused" />
                </Border>
            </Grid>

            <!-- ═══ Orta: stage/unstage araç çubuğu ═══ -->
            <Grid Grid.Row="1" ColumnDefinitions="Auto,*,Auto" Margin="0,6">

                <!-- Sol grup: staged listesinden geri alma -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="4">
                    <Button Name="UnstageAllButton"
                            Content="⏫"
                            ToolTip.Tip="Tümünü geri al"
                            FontSize="11"
                            Padding="8,3"
                            Click="OnUnstageAllClick" />

                    <Border Width="1"
                            Margin="2,2"
                            Background="{DynamicResource SystemControlForegroundBaseLowBrush}" />

                    <Button Name="UnstageButton"
                            Content="⬆ Geri al"
                            ToolTip.Tip="Seçili dosyayı geri al"
                            FontSize="11"
                            Padding="8,3"
                            Click="OnUnstageClick" />
                </StackPanel>

                <!-- Sağ grup: unstaged listesinden stage'leme -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="4">
                    <Button Name="StageButton"
                            Content="⬇ Stage"
                            ToolTip.Tip="Seçili dosyayı stage'le"
                            FontSize="11"
                            Padding="8,3"
                            Click="OnStageClick" />

                    <Border Width="1"
                            Margin="2,2"
                            Background="{DynamicResource SystemControlForegroundBaseLowBrush}" />

                    <Button Name="StageAllButton"
                            Content="⏬"
                            ToolTip.Tip="Tümünü stage'le"
                            FontSize="11"
                            Padding="8,3"
                            Click="OnStageAllClick" />
                </StackPanel>
            </Grid>

            <!-- ═══ Alt: Staged ═══ -->
            <Grid Grid.Row="2" RowDefinitions="Auto,*,Auto">

                <TextBlock Grid.Row="0"
                           Name="StagedCaption"
                           Text="{Binding Staged.Count, StringFormat='Staged changes ({0})'}"
                           FontWeight="SemiBold"
                           FontSize="12"
                           Margin="2,0,0,4" />

                <Border Grid.Row="1"
                        BorderThickness="1"
                        BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}">

                    <ListBox Name="StagedList"
                             ItemsSource="{Binding Staged}"
                             SelectedIndex="{Binding SelectedStagedIndex}"
                             ItemTemplate="{StaticResource WorkingTreeFileTemplate}"
                             Background="Transparent"
                             BorderThickness="0"
                             GotFocus="OnStagedFocused" />
                </Border>

                <!-- Hata şeridi + git'in tam çıktısı (P05-T07) -->
                <StackPanel Grid.Row="2"
                            Orientation="Horizontal"
                            Spacing="8"
                            Margin="0,6,0,0"
                            IsVisible="{Binding ErrorMessage, Converter={x:Static ObjectConverters.IsNotNull}}">

                    <TextBlock Text="{Binding ErrorMessage}"
                               Foreground="{DynamicResource SystemFillColorCriticalBrush}"
                               TextWrapping="Wrap"
                               VerticalAlignment="Center" />

                    <views:GitErrorDetailsButton Details="{Binding ErrorDetails}" />
                </StackPanel>
            </Grid>
        </Grid>

        <GridSplitter Grid.Column="1" Width="6" Background="Transparent" />

        <!-- ================= SAĞ SÜTUN ================= -->
        <!--
            `splitRight` (yatay): Panel1 = SelectedDiff, Panel2 = commit mesajı.
            SplitterDistance 426 / 622 ≈ %68.
        -->
        <Grid Grid.Column="2" RowDefinitions="68*,Auto,32*">

            <views:DiffView Grid.Row="0" DataContext="{Binding Diff}" />

            <GridSplitter Grid.Row="1" Height="6" Background="Transparent" />

            <!--
                `tableLayoutPanel1` (2×2): sol sütun `flowCommitButtons` (otomatik genişlik),
                sağ sütunda üstte `toolbarCommit`, altta `Message`.
            -->
            <Grid Grid.Row="2" ColumnDefinitions="Auto,*" RowDefinitions="Auto,*" Margin="0,4,0,0">

                <!--
                    `flowCommitButtons` sırası birebir: Commit · Commit&amp;Push ·
                    Stage in Superproject · Amend · [Reset author · Reset soft] ·
                    Stash staged · Reset all changes · Reset unstaged changes.
                    Uygulanmamış olanlar devre dışı ama YERİNDE (§ 9).
                -->
                <StackPanel Grid.Row="0" Grid.RowSpan="2" Grid.Column="0"
                            Spacing="4"
                            Margin="0,0,10,0"
                            MinWidth="150">

                    <Button Name="CommitButton"
                            Content="Commit"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Center"
                            IsEnabled="{Binding CanCommit}"
                            ToolTip.Tip="Ctrl+Enter"
                            Click="OnCommitClick" />

                    <Button Name="CommitAndPushButton"
                            Content="Commit &amp; push"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Center"
                            IsEnabled="False" />

                    <CheckBox Name="StageInSuperprojectBox"
                              Content="Üst projede stage'le"
                              FontSize="11"
                              IsEnabled="False" />

                    <CheckBox Name="AmendBox"
                              Content="Son commit'i düzelt (amend)"
                              FontSize="11"
                              IsChecked="{Binding Amend}" />

                    <StackPanel Margin="12,0,0,0" Spacing="2" IsVisible="{Binding Amend}">
                        <CheckBox Name="ResetAuthorBox" Content="Yazarı sıfırla" FontSize="11" IsEnabled="False" />
                        <CheckBox Name="ResetSoftBox" Content="Soft reset" FontSize="11" IsEnabled="False" />
                    </StackPanel>

                    <Button Name="StashStagedButton" Content="Stage'lenmişi stash'le"
                            HorizontalAlignment="Stretch" HorizontalContentAlignment="Center"
                            FontSize="11" IsEnabled="False" />

                    <Button Name="ResetAllButton" Content="Tüm değişiklikleri sıfırla…"
                            HorizontalAlignment="Stretch" HorizontalContentAlignment="Center"
                            FontSize="11" IsEnabled="False" />

                    <Button Name="ResetUnstagedButton" Content="Stage'lenmemişleri sıfırla…"
                            HorizontalAlignment="Stretch" HorizontalContentAlignment="Center"
                            FontSize="11" IsEnabled="False" />
                </StackPanel>

                <!--
                    `toolbarCommit` sırası: commit message ▾ · options ▾ · templates ▾ ·
                    create branch. Hepsi P05-T13 / Faz 06.
                -->
                <StackPanel Grid.Row="0" Grid.Column="1"
                            Orientation="Horizontal"
                            Spacing="6"
                            Margin="0,0,0,4">

                    <!--
                        Geçmiş menüsü AÇILIRKEN okunuyor (GitExtensions'ta da
                        `DropDownOpening`): her depo açılışında fazladan bir `git log`
                        çalıştırmak, kullanıcının hiç açmayacağı bir menünün bedeli olurdu.
                    -->
                    <Button Name="MessageHistoryButton" Content="Mesaj ▾" FontSize="11" Padding="8,2">
                        <Button.Flyout>
                            <MenuFlyout Opening="OnMessageHistoryOpening">
                                <MenuItem Header="Yalnızca benim mesajlarım"
                                          ToggleType="CheckBox"
                                          IsChecked="{Binding Message.OnlyMyMessages}" />
                                <Separator />
                            </MenuFlyout>
                        </Button.Flyout>
                    </Button>

                    <Button Name="CommitOptionsButton" Content="Seçenekler ▾" FontSize="11" Padding="8,2" IsEnabled="False" />

                    <Button Name="CommitTemplatesButton" Content="Şablonlar ▾" FontSize="11" Padding="8,2">
                        <Button.Flyout>
                            <MenuFlyout>
                                <!--
                                    Bulunamayan şablon GİZLENMİYOR: git'in kendisi o durumda
                                    commit'i `fatal: could not read` ile reddediyor, yani
                                    yapılandırma gerçekten bozuk (ölçüldü).
                                -->
                                <MenuItem Name="TemplateItem"
                                          Header="{Binding Message.TemplateLabel}"
                                          IsEnabled="{Binding Message.CanApplyTemplate}"
                                          Click="OnApplyTemplateClick" />
                            </MenuFlyout>
                        </Button.Flyout>
                    </Button>
                    <Button Name="CreateBranchButton" Content="Dal oluştur" FontSize="11" Padding="8,2" IsEnabled="False" />

                    <TextBlock Name="SubjectCounter"
                               Text="{Binding Message.SubjectCounter}"
                               Classes.over="{Binding Message.IsSubjectTooLong}"
                               FontSize="11"
                               VerticalAlignment="Center"
                               Margin="8,0,0,0" />
                </StackPanel>

                <!--
                    Mesaj kutusu + 50/72 kılavuzu. Kılavuz kutunun ARKASINDA çiziliyor;
                    metnin üstüne çizmek imleci ve seçimi gölgelerdi.
                -->
                <Grid Grid.Row="1" Grid.Column="1" RowDefinitions="*,Auto">

                    <Border Grid.Row="0"
                            BorderThickness="1"
                            BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}">

                        <Panel>
                            <controls:ColumnGuides Name="MessageGuides"
                                                   Columns="{Binding Message.GuideColumnsForBinding}"
                                                   Stroke="#D0D7DE"
                                                   TextOffset="5"
                                                   TextElement.FontFamily="Cascadia Mono,Consolas,Menlo,DejaVu Sans Mono,monospace"
                                                   TextElement.FontSize="12" />

                            <TextBox Name="MessageBox"
                                     Text="{Binding Message.Text}"
                                     AcceptsReturn="True"
                                     AcceptsTab="False"
                                     TextWrapping="NoWrap"
                                     Background="Transparent"
                                     BorderThickness="0"
                                     Padding="4,0"
                                     FontFamily="Cascadia Mono,Consolas,Menlo,DejaVu Sans Mono,monospace"
                                     FontSize="12"
                                     VerticalContentAlignment="Top"
                                     PlaceholderText="Commit mesajı (Ctrl+Enter ile commit)" />
                        </Panel>
                    </Border>

                    <TextBlock Grid.Row="1"
                               Name="MessageHint"
                               Text="{Binding Message.Hint}"
                               IsVisible="{Binding Message.HasHint}"
                               Foreground="{DynamicResource SystemFillColorCautionBrush}"
                               FontSize="11"
                               TextWrapping="Wrap"
                               Margin="2,3,0,0" />
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
