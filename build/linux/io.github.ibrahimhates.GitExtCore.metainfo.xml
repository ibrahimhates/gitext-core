<?xml version="1.0" encoding="UTF-8"?>
<!--
  AppStream metadata (P10-T07). Required by Flathub and by software centres (GNOME Software,
  KDE Discover, elementary AppCenter).

  The file name must be THE SAME as the application id: io.github.ibrahimhates.GitExtCore
  (settled in P01-T21, and it matches the .desktop file too).

  ⚠️ The screenshot URLs are filled in with real files in P10-T26. AppStream does not REQUIRE
  a screenshot, but software centres are reluctant to list applications without one; it is
  essential before submitting to Flathub.
-->
<component type="desktop-application">
  <id>io.github.ibrahimhates.GitExtCore</id>

  <name>gitext-core</name>
  <summary>Browse history, stage changes and manage branches in Git repositories</summary>

  <metadata_license>CC0-1.0</metadata_license>
  <project_license>GPL-3.0-or-later</project_license>

  <developer id="io.github.ibrahimhates">
    <name>gitext-core contributors</name>
  </developer>

  <description>
    <p>
      gitext-core is a fast, native Git GUI for Linux, Windows and macOS. It rebuilds the
      GitExtensions experience on modern .NET and Avalonia: a commit graph that makes tangled
      histories readable, a UI that maps onto how Git actually works, and speed that stays out
      of your way.
    </p>
    <p>
      It drives the real <code>git</code> command line rather than a reimplementation of it, so
      your hooks, aliases, credential helpers and configuration all behave exactly as they do in
      a terminal. Every operation shows the underlying command it ran.
    </p>
    <p>What it does:</p>
    <ul>
      <li>Read a commit graph of hundreds of thousands of commits without waiting</li>
      <li>Stage and unstage by file, hunk or individual line</li>
      <li>Commit, branch, merge, rebase (including interactive), cherry-pick and revert</li>
      <li>Fetch, pull and push, with force-with-lease anchored to what you actually saw</li>
      <li>Resolve conflicts, manage stashes, browse the reflog, blame and file history</li>
      <li>Light and dark themes, a colour-blind-safe graph palette, and reassignable shortcuts</li>
    </ul>
    <p>
      gitext-core collects no telemetry of any kind.
    </p>
    <p>
      This is a clean-room reimplementation inspired by GitExtensions. It is not a port of it and
      is not affiliated with the GitExtensions project.
    </p>
  </description>

  <launchable type="desktop-id">io.github.ibrahimhates.GitExtCore.desktop</launchable>

  <url type="homepage">https://github.com/ibrahimhates/gitext-core</url>
  <url type="bugtracker">https://github.com/ibrahimhates/gitext-core/issues</url>
  <url type="vcs-browser">https://github.com/ibrahimhates/gitext-core</url>

  <categories>
    <category>Development</category>
    <category>RevisionControl</category>
  </categories>

  <keywords>
    <keyword>git</keyword>
    <keyword>vcs</keyword>
    <keyword>version control</keyword>
    <keyword>commit</keyword>
    <keyword>branch</keyword>
    <keyword>merge</keyword>
    <keyword>diff</keyword>
  </keywords>

  <!--
    A desktop application: a keyboard and a pointer are needed, touch is not supported.
    Software centres use this information to show device compatibility.
  -->
  <supports>
    <control>keyboard</control>
    <control>pointing</control>
  </supports>

  <requires>
    <display_length compare="ge">768</display_length>
  </requires>

  <content_rating type="oars-1.1" />

  <!--
    The release history. A new <release> has to be added here for every new version;
    it is part of the release checklist in P10-T29.
  -->
  <!--
    ⚠️ A NEW <release> has to be added here on every release; the newest AT THE TOP.
    Software centres show this list as "what changed", and when it is missing the user cannot
    see the release history at all.

    `type="development"`: not a stable release. Leaving it out would mean software centres
    listing a 0.x version as though it were stable.
  -->
  <releases>
    <release version="0.1.0" date="2026-08-15" type="development">
      <description>
        <p>
          First packaged release. Git works end to end: browsing history, diffing, staging by
          line, committing, branching, fetch/pull/push, rebase, cherry-pick, revert, stash and
          conflict resolution.
        </p>
        <p>
          The interface is English by default, with Turkish available and switchable at runtime.
        </p>
        <p>
          Linux is exercised daily and every package format is verified in a clean container.
          Windows runs but has not been used as a daily driver; macOS has not been run on real
          hardware.
        </p>
      </description>
      <url type="details">https://github.com/ibrahimhates/gitext-core/releases/tag/v0.1.0</url>
    </release>
  </releases>
</component>
