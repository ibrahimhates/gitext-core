# Benchmark temel çizgisi — 2026-08-10

Bu dosya **izleniyor**. Sonraki koşular buna göre karşılaştırılır (P09-T02).
Ham çıktı (`BenchmarkDotNet.Artifacts/`) yoksayılıyor — her koşuda yeniden üretiliyor.

| | |
|---|---|
| **Ortam** | Linux x64, .NET 10.0.10, RyuJIT x86-64-v3 |
| **BenchmarkDotNet** | 0.15.6 |
| **Koşu kipi** | `--fast` (1 warmup, 3 iterasyon) |
| **Commit** | P09-T02 tamamlandığında |

> ⚠️ **Bu sayılar `--fast` ile alındı — N=3.** Regresyon eşiği olarak kullanmak için
> dar bir güven aralığı gerekiyor; tam koşu (varsayılan job, N≈15–100) yapılmadan
> **%10'un altındaki farklar gürültüden ayırt edilemez.** İlk gerçek eşik P09-T04'te
> tam koşuyla belirlenecek.

---

## Graph yerleşimi (`GitExt.Graph`)

Girdi `DagFixture.Parse` ile kuruluyor, ölçülen `GraphLayoutEngine.Add`.

| Benchmark | Commit | Şerit | Ortalama |
|---|---:|---:|---:|
| `Linear_10k` | 10.000 | 1 | **3.428 ms** |
| `Linear_50k` | 50.000 | 1 | **22.893 ms** |
| `Branched_10k` | 9.999 | 2 | **3.637 ms** |
| `Merged_10k` | 10.000 | 2 | **3.894 ms** |
| `MultiMerge_10k` | 10.002 | 4 | **4.222 ms** |

**Okunan:** 10k'da topoloji maliyeti dar — lineerden octopus'a %23 fark. Ölçek ise
süperlineer: 5× girdi 6.7× süre (3.428 → 22.893 ms). Büyüme kaynağı P09-T04'te
profillenecek; şu an bunun algoritmadan mı yoksa tahsisten mi geldiği **bilinmiyor**.

## Model (`GitExt.Core.Model`)

| Benchmark | Ortalama |
|---|---:|
| `Generate_10k` — 10k `CommitInfo` tahsisi | **4.637 ms** |
| `RetainedMemory_10k` — commit + `GraphRow` birlikte | **7.233 ms** |
| `MeasureTextWeight` — 10k commit metin baytı | **50.699 µs** |
| `ParseRefsEmpty` — 100k boş çağrı | **31.570 µs** |
| `ParseRefsOne` — 100k tek ref | **2.354 ms** |
| `ParseRefsMany` — 100k, 5 ref | **7.604 ms** |
| `FindUniqueAuthors` — 10k imzada benzersiz yazar | **54.125 µs** |

**Okunan:** `ParseRefs` boş girdide bedava (315 ns/çağrı), ama ref sayısıyla doğrusal
büyüyor — beş ref 3.2× pahalı. 10k commit'lik bir ekranda toplam ~760 µs, şu an sorun
değil; `Split` tahsisi P09-T08'in adayı.

## Parser (`CommitId`)

| Benchmark | Ortalama |
|---|---:|
| `ParseSingleRecord` | **339.570 ns** |
| `CompareAll` | **7.287 µs** |
| `ShortenAll` | **171.007 µs** |
| `TryParseAll` | **283.156 µs** |

---

## Yeniden üretme

```
dotnet run --project benchmarks/GitExt.Benchmarks -c Release -- --fast
```

Tek sınıf: `--filter "*Linear*"` · tek metot: `--filter "LinearBenchmarks.Linear_50k"`
